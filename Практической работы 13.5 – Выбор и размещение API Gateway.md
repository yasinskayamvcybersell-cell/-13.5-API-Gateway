# Решение практической работы 13.5 – Выбор и размещение API Gateway

## Контекст
На основе предыдущих работ (2.6–12.5) спроектирована распределённая система микросервисов (Orders, Inventory, Pricing, Payments, Shipping, Cart, Notifications, Audit, Catalog, ExperimentProcessor). Внедрены:
- **API Gateway** для внешних пользователей (JWT, rate limiting, WAF, mTLS) – из задания 12.5.
- Событийная шина (Kafka), саги, OpenAPI.

**Новые требования**:  
Появились IoT устройства (станки, датчики контроля качества, системы учёта движения на складах), а также растёт сеть дистрибьюторов и пользователей. Необходимо собирать и обрабатывать телеметрию, принимать решения на основе данных, избегая хаоса в управлении потоками.

---

## 1. Архитектурная диаграмма с расположением API Gateway

```mermaid
flowchart LR
    subgraph Internet [Интернет]
        Users[Пользователи B2C]
        Distributors[Дистрибьюторы B2B]
        IoT_Devices[IoT устройства<br>станки, датчики, сканеры]
    end

    subgraph DMZ [Публичная зона (DMZ)]
        direction LR
        PubGW[Public API Gateway]
        B2BGW[B2B API Gateway]
        IoTGW[IoT API Gateway<br>MQTT/WebSocket]
    end

    subgraph Private [Приватная подсеть]
        direction LR
        
        subgraph Services [Микросервисы]
            direction TB
            Orders[Orders]
            Inventory[Inventory]
            Pricing[Pricing]
            Payments[Payments]
            Shipping[Shipping]
            Catalog[Catalog]
            Notifications[Notifications]
            Experiment[ExperimentProcessor]
        end
        
        subgraph IoT_Processing [Обработка телеметрии]
            direction TB
            TelemetryProc[Telemetry Processor]
            RulesEngine[Rules Engine]
        end
        
        subgraph Data [Слои данных]
            direction TB
            DB_Orders[(RDBMS Orders)]
            DB_Inventory[(RDBMS Inventory)]
            Kafka[(Kafka Event Bus)]
            Redis[(Redis KV)]
        end
        
        subgraph Security [Безопасность]
            direction TB
            Vault[HashiCorp Vault]
            OPA[Open Policy Agent]
        end
    end

    %% Связи (прямые линии, только горизонтальные и вертикальные)
    Users --> PubGW
    Distributors --> B2BGW
    IoT_Devices -->|MQTT/HTTP| IoTGW

    PubGW -->|mTLS| Services
    B2BGW -->|mTLS + API Key| Services
    IoTGW -->|gRPC/HTTP| TelemetryProc

    TelemetryProc --> Kafka
    TelemetryProc --> RulesEngine
    RulesEngine -->|команды| IoTGW
    IoTGW -->|управление| IoT_Devices

    Services --> Kafka
    Services --> DB_Orders
    Services --> DB_Inventory
    Services --> Redis

    Vault -->|секреты| PubGW
    Vault -->|секреты| B2BGW
    Vault -->|секреты| IoTGW
    OPA -->|политики| PubGW
    OPA -->|политики| B2BGW